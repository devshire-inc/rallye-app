/**
 * A1 — Login. Stub de rota: a tela real (BEAC-1793) ainda não existe neste
 * repositório. Este placeholder existe apenas para que o link "Fazer login?"
 * do cadastro (A2, BEAC-1788), mostrado quando o e-mail já está cadastrado,
 * tenha um destino válido.
 */
export function LoginPage() {
  return (
    <section style={{ maxWidth: 420, margin: '0 auto', padding: '32px 16px', textAlign: 'left' }}>
      <h1>Entrar</h1>
      <p>Tela de login em construção (BEAC-1793).</p>
    </section>
  )
}

export default LoginPage
